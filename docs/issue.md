航空公司視角：
 手機版才要出現收河展開按鈕，且上方tab和收和展開按鈕要sticky在最上方。手機版狀態badge要維持橫的一行不要變成直的，操作文字內容也不要折行。櫃檯分頁那邊 調整範圍 整季/單日 被換行了要維持同一行。報表分頁日期選擇器跟上下切換按鈕要在同一行，點選任一星期欄位,可跳至該日全櫃檯檢視文字移除，當日全六櫃檯排程 · 依時間排序文字移除。當日那邊不需要在櫃檯旁邊顯示0 筆，當日排程(依時間排序)改成顯示日期，當日無排程改成顯示 - 。
